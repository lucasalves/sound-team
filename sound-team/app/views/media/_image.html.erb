<div class="media-item" id="media-item-<%= item.id %>">
    <%= link_to(screenshot(item), item) %>
    <h3><%= link_to(item.name, item) %></h3>
    <span class="size"><%= dimensions(item) %></span>
    <%= render :partial => "buttons", :locals => {:item => item, :size => "xs"} %>
</div>
