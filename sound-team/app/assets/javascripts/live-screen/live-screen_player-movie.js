(function(LiveScreen){
  LiveScreen.PlayerMovie = function(html, emitter){
    this.html    = html;
    this.emitter = emitter;
  };

  LiveScreen.PlayerMovie.prototype.set = function(data){
    console.log('todo movie');
  };
}(LiveScreen));